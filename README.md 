# Kynium

Kynium — это новый язык программирования, который позволяет быстро разрабатывать приложения с простым и лаконичным синтаксисом.

## Установка

1. Клонируйте репозиторий:
git clone https://github.com/your-username/kynium.git

2. Перейдите в каталог проекта и выполните скрипт установки:
cd kynium ./install.sh

3. Теперь вы можете запускать программу с помощью команды:
kyn run examples/hello.kyn


## Пример

Пример использования языка:

```kynium
let x = 10
let y = 20
let result = x + y
print(result)
